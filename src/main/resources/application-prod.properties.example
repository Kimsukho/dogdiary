# ============================================
# 운영 환경 설정 예시 파일
# 이 파일을 복사하여 application-prod.properties로 이름을 변경하고
# 실제 운영 환경 값으로 수정하세요.
# application-prod.properties는 .gitignore에 포함되어 Git에 커밋되지 않습니다.
# ============================================

# --- 데이터베이스 설정 (운영 환경) ---
spring.datasource.url=jdbc:mysql://your-production-db-host:3306/your_database?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=your_db_username
spring.datasource.password=your_db_password

# --- JWT 설정 ---
# 운영 환경에서는 반드시 강력한 Secret 키를 사용하세요!
jwt.secret=your-strong-jwt-secret-key-here

# --- 로깅 설정 (운영 환경) ---
# 운영 환경에서는 보안을 위해 DEBUG 레벨을 사용하지 않는 것을 권장합니다.
logging.level.org.springframework.security=INFO
logging.level.com.project=INFO

# --- Thymeleaf 캐시 설정 (운영 환경) ---
# 운영 환경에서는 캐시를 활성화하여 성능을 향상시킵니다.
spring.thymeleaf.cache=true

# --- 정적 리소스 캐시 설정 (운영 환경) ---
spring.web.resources.cache.period=3600
spring.web.resources.cache.cachecontrol.no-cache=false

# --- H2 콘솔 비활성화 (운영 환경) ---
spring.h2.console.enabled=false

